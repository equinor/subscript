- name: Gas-oil-ratio observations
  type: timeseries
  error: 10% # optional (% indicates relative, otherwise absolute)
  min_error: 20 # optional, always absolute
  max_error: 30 # optional, always absolute
  # Columns: vector, date, value, error, min_error, max_error (last three columns optional for each row, overrides global error for the rows where they are presnet)
  observation: "./input/observations/summary_gor.csv"

- name: Bottom hole pressure observations
  type: timeseries
  error: 5%
  observation: "./input/observations/summary_bhp.csv"

- name: RFT pressure observations
  type: rft # we have a plugin that "knows about rft"
  plugin_arguments:
    zonemap: "./rms/output/zone/layer_zone_table.txt"
    trajectories: "./rms/output/trajectories.csv"
  error: 5
  # Columns: well, date, measured_depth, zone, value, error, min_error, max_error (last three columns optional)
  observation: "./input/observations/rft_pressure.csv"
  metadata:
    columns:
      measured_depth:
        unit: m
        reference: msl
      value:
        unit: bar

- name: RFT saturation observations
  type: rft # we have a plugin that "knows about rft"
  plugin_arguments:
    zonemap: "./rms/output/zone/layer_zone_table.txt"
    trajectories: "./rms/output/trajectories.csv"
  error: 5
  # Columns: well, date, measured_depth, zone, value, error, min_error, max_error (last three columns optional)
  observation: "./input/observations/rft_saturation.csv"
  metadata:
    columns:
      measured_depth:
        unit: m
        reference: msl
      value:
        unit: fraction
# - # name: 4D gravity
# type: gravity
# observation: "./"
# observations:
#   # gravity plugin makes the N^2 combinations
#   - value: 100
#     error: 5
#     coordinate:
#       station_name: xxx
#       date: 2003-01-01
