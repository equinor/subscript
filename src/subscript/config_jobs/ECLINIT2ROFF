EXECUTABLE convert_grid_format 

ARG_TYPE 0 STRING
ARG_TYPE 1 STRING
ARG_TYPE 2 STRING

ARGLIST "--conversion" ecl2roff "--file" <ECLROOT> "--output" <OUTPUT> "--propnames" <PROP>  "--mode" init "--standardfmu"

MIN_ARG 3
MAX_ARG 3
